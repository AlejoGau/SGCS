Ext.define('SgAppMapGuardWeb.controller.MapGuardWebController', {
    extend : 'Ext.app.Controller',
    stores : [  ],
    models : [  ],
    views : [ 'ExtUxNotification' ],

    init : function(config) {
		// genero los eventos
	/*	this.control({
            'viewport' : {
                afterrender : this.initview
			}
		});*/
	}, // cierro init
    
   /* initview: function(viewport){
       viewport.down('#datapanel').collapse();
    },

    openObjectList: function(viewport){

	},

	openObjectById : function(objectId) {
        notityError('Funcion no soportada');
	}*/
});